import 'package:flutter/material.dart';

class SizedBox16 extends StatelessWidget {
  const SizedBox16({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => const SizedBox(height: 16);
}

// ignore: prefer-single-widget-per-file
class SizedBox8 extends StatelessWidget {
  const SizedBox8({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => const SizedBox(height: 8);
}

// ignore: prefer-single-widget-per-file
class SizedBox24 extends StatelessWidget {
  const SizedBox24({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => const SizedBox(height: 24);
}

// ignore: prefer-single-widget-per-file
class SizedBox36 extends StatelessWidget {
  const SizedBox36({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => const SizedBox(height: 36);
}

// ignore: prefer-single-widget-per-file
class SizedBox4 extends StatelessWidget {
  const SizedBox4({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => const SizedBox(height: 4);
}
