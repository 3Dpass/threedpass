import 'package:flutter/material.dart';

class CreateAccountMnemonicGen extends StatelessWidget {
  const CreateAccountMnemonicGen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('m'),
      ),
    );
  }
}
