part of '../create_swap_page.dart';

class _ChooseDeadline extends StatelessWidget {
  const _ChooseDeadline();

  @override
  Widget build(final BuildContext context) {
    // showDatePicker(context: context, firstDate: firstDate, lastDate: lastDate)
    // showCupertinoModalPopup(context: context, builder: CupertinoDatePicker(
    //                     mode: CupertinoDatePickerMode.date,
    //                     onDateTimeChanged: (value) {
    //                       setState(() {
    //                         date = value;
    //                       });
    //                     },
    //                   ),)
    return SizedBox();
  }
}
