void deleteDB() {
  // final dir = await getApplicationDocumentsDirectory();
  // final files = dir.listSync();
  // print(files.map((final f) => f.path));
  // final db = files.firstWhere(
  //   (final f) => f.path.endsWith('.sqlite'),
  // );
  // print(db.path);
  // db.deleteSync();
}
