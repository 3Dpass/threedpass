targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: threedp_graphql|lib/core/graphql/threedpass.schema.graphql
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: threedp_graphql|lib/core/graphql/threedpass.schema.graphql